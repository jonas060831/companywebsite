<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Us</title>
</head>
<body>
    <%- include('./partials/nav') %> <!-- add nav bar WOO -->
    <h1>Meet Our Team!</h1>
        <ul>
            <% staff.forEach(person => { %>
                <li>
                    <img src="<%= person.image %>" />
                    <strong><%= person.name %></strong>
                    <ul> Role: <%= person.jobTitle %></ul>
                    <ul> Email: <%= person.email %></ul>
                    <ul> Number: <%= person.phoneNumber %></ul>
                    <ul> Bio: <%= person.bio %></ul>
                </li>
               <% }) %>
        </ul>
</body>
</html>